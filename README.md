# Scala Example

Using this as a testbed for basic scala package structure. Implementation of a least-recently used cache and some other operations just for exploration.

## Usage
- `sbt test` to run unit tests
- `sbt run` to run the Main class
